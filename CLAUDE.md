## Environment & Config

- Claude Code config dir: `CLAUDE_CONFIG_DIR="${XDG_CONFIG_HOME:-$HOME/.config}/claude"` (~/.config/zsh/.zshenv)
- `~/.claude`는 사용하지 않음. 모든 Claude 설정은 `~/.config/claude/`에 위치.

## Git

- 커밋 메시지에 `Co-Authored-By` 등 Claude 관련 Co-Author 정보를 절대 포함하지 않는다.
